---
title: Checkbox
description: A custom checkbox component for forms and selection lists, with support for indeterminate states and smooth toggle animations.
kind: component
category: Components
image: /logo.png
dependencies:
  - react-native
install:
  - watermelon add checkbox
qrValue: showcase://components/checkbox
sourceHref: https://github.com/vanshpatelx/RN/blob/main/packages/registry/src/components/ui/checkbox.tsx
---

import { CodeBlock } from "@/components/showcase/code-block";
import {
  ApiTable,
  DocSection,
  DocSubsection,
} from "@/components/showcase/docs-primitives";
import { CompPreview } from "@/components/mdx/component-preview";
import {
  CheckboxBasicInlinePreview,
  CheckboxDisabledInlinePreview,
} from "@/components/showcase/component-live-preview";

## Import

<DocSection>
  <CodeBlock language="tsx" title="Import" showLineNumbers={false}>
    {`import { Checkbox } from "@/components/ui/checkbox"`}
  </CodeBlock>
</DocSection>

## Usage

<DocSection>
  The `Checkbox` component is designed for multi-select scenarios and agreement forms. It uses a lightweight native checkmark indicator and supports a fully controlled state.

### Basic Usage

<DocSubsection>
  Bind the checkbox to a boolean state to handle user selections.
  <CompPreview
    code={`const [checked, setChecked] = useState(false);
return <Checkbox checked={checked} onCheckedChange={setChecked} />;`}
  >
    <CheckboxBasicInlinePreview />
  </CompPreview>
</DocSubsection>

### Disabled State

<DocSubsection>
  Use the `disabled` prop to prevent interactions.
  <CompPreview
    code={`<Checkbox disabled />
<Checkbox checked disabled />`}
  >
    <CheckboxDisabledInlinePreview />
  </CompPreview>
</DocSubsection>
</DocSection>

## API Reference

<DocSection>
  `Checkbox` extends all supported props from React Native `Pressable`.
  <ApiTable
    rows={[
      {
        prop: "checked",
        type: "boolean",
        default: "false",
        description: "The controlled checked state of the checkbox.",
      },
      {
        prop: "onCheckedChange",
        type: "(checked: boolean) => void",
        default: "-",
        description: "Callback called when the checked state changes.",
      },
      {
        prop: "disabled",
        type: "boolean",
        default: "false",
        description: "Whether the checkbox is interactive.",
      },
    ]}
  />
</DocSection>
