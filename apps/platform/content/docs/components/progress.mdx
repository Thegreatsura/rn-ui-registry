---
title: Progress
description: An indicator showing the completion status of a task, typically displayed as a horizontal bar that fills as the task progresses.
kind: component
category: Components
image: /logo.png
dependencies:
  - react-native
install:
  - watermelon add progress
qrValue: showcase://components/progress
sourceHref: https://github.com/vanshpatelx/RN/blob/main/packages/registry/src/components/ui/progress.tsx
---

import { CodeBlock } from "@/components/showcase/code-block";
import {
  ApiTable,
  DocSection,
  DocSubsection,
} from "@/components/showcase/docs-primitives";
import { CompPreview } from "@/components/mdx/component-preview";
import {
  ProgressBasicInlinePreview,
  ProgressIndeterminateInlinePreview,
} from "@/components/showcase/component-live-preview";

## Import

<DocSection>
  <CodeBlock language="tsx" title="Import" showLineNumbers={false}>
    {`import { Progress } from "@/components/ui/progress"`}
  </CodeBlock>
</DocSection>

## Usage

<DocSection>
  The `Progress` component uses React Native's `Animated` API to provide smooth, hardware-accelerated transitions. It is perfect for file uploads, download status, or multi-step onboarding flows.

### Basic Usage

<DocSubsection>
  Progress bars accept a `value` and optional `max` (default is 100).
  <CompPreview
    code={`<Progress value={45} />
<Progress value={80} />`}
  >
    <ProgressBasicInlinePreview />
  </CompPreview>
</DocSubsection>

### Full Width

<DocSubsection>
  The progress bar automatically fills the width of its container. Use `className` to adjust the track appearance.
  <CompPreview
    code={`<Progress value={100} className="bg-primary/20" />`}
  >
    <ProgressIndeterminateInlinePreview />
  </CompPreview>
</DocSubsection>
</DocSection>

## API Reference

<DocSection>
  `Progress` extends all supported props from React Native `View`.
  <ApiTable
    rows={[
      {
        prop: "value",
        type: "number",
        default: "0",
        description: "The current progress value.",
      },
      {
        prop: "max",
        type: "number",
        default: "100",
        description: "The maximum value of the progress bar.",
      },
      {
        prop: "className",
        type: "string",
        default: "-",
        description: "Standard Tailwind classes for the track.",
      },
    ]}
  />
</DocSection>
